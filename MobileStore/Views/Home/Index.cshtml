@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered Mobiles of the week </h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var mobile in Model.PreferredMobiles)
    {
        @Html.Partial("MobilesSummary", mobile)
    }
</div>